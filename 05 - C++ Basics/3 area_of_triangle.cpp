#include<iostream>
using namespace std;

int main(){
    float b, h, a;
    cout << "enter base and height"<<endl;
    cin >> b >> h;
    //formula
    a = (b * h) / 2;
    cout << "Area of triangle is " << a << endl;
}